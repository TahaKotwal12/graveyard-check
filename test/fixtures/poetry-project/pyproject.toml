[project]
name = "demo-poetry-app"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["requests (>=2.31.0,<3.0.0)", "Flask-Script[extras] (>=2.0.6)"]

[tool.poetry]
packages = []

[tool.poetry.group.dev.dependencies]
pytest = "^8.0"

[build-system]
requires = ["poetry-core>=2.0.0"]
build-backend = "poetry.core.masonry.api"
